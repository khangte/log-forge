# -----------------------------------------------------------------------------
# 패키지 : simulator
# 목적   : 로그 시뮬레이터 최상위 패키지
# 구성   : core(설정/유틸), services(도메인별 생성기),
#          generator(서비스 호출 어댑터), kafka_producer(Kafka 발행 어댑터), main(엔트리)
# -----------------------------------------------------------------------------
