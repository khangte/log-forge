# -----------------------------------------------------------------------------
# 패키지 : simulator.cli
# 목적   : 커맨드라인 인자 파싱 유틸을 제공하는 하위 패키지
# 사용   : main.py 또는 runner를 호출하는 진입점에서 args.parse_args() 사용
# 주의   : argparse만 의존. 파일 경로 기본값은 core.config에서 가져온다.
# -----------------------------------------------------------------------------
