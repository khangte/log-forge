# -----------------------------------------------------------------------------
# 패키지 : simulator
# 목적   : 로그 시뮬레이터 최상위 패키지
# 구성   : core(설정/러너/유틸), services(도메인별 생성기), cli(인자 파싱),
#          generator(서비스 호출 어댑터), producer(Kafka 발행 어댑터), main(엔트리)
# -----------------------------------------------------------------------------
